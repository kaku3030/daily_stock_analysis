# Radar

Stock Razor's candidate-discovery boundary. The production implementation remains under `src/services/screening/` and `src/services/stock_radar_v2/` during this compatibility migration.

Radar may discover and rank opportunities. It must not infer that a candidate is held or mutate portfolio state.
