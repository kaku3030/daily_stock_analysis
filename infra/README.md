# Infra

Deployment, scheduling, and observability boundary. Existing production assets remain in `.github/workflows/`, `docker/`, and `scripts/` until each consumer can be moved without changing its path contract.
